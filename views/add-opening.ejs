<!DOCTYPE html>
<html lang="en">

<head>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
        integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add/Update Opening</title>
</head>
<style>
    .container {
        display: flex;
        justify-content: center;
        padding-top: 15%;
    }

    .add-update-card {
        width: 600px;
    }
</style>

<body>
    <%- include('nav', {title: title}); %>

    <div class="container">
        <div class="card add-update-card">
            <div class="card-header">
                <h2>Add/Update Opening</h2>
            </div>
            <div class="card-body">
                <form action="/openings/add" method="POST">
                    <% if(opening._id) { %>
                    <input type="hidden" name="_id" value='<%- opening._id %>' />
                    <% } %>
                    <div class="form-group">
                        <input name="project" type="text" placeholder="Project Name" class="form-control"
                            value="<%- opening.project %>" />
                    </div>

                    <div class="form-group">
                        <input name="client" type="text" placeholder="Client Name" class="form-control"
                            value="<%- opening.client %>" />
                    </div>

                    <div class="form-group">
                        <input name="technologies" type="text" placeholder="Technologies" class="form-control"
                            value="<%- opening.technologies %>" />
                    </div>

                    <div class="form-group">
                        <label for="role">Status</label>
                        <select name="role" id="role" class="form-control">
                            <option>Frontend Developer</option>
                            <option>Backend Developer</option>
                            <option>Quality Analyst</option>
                            <option>Business Analyst</option>
                            <option>Data Analyst</option>
                        </select>
                    </div>

                    <div class="form-group">
                        <input name="description" type="text" placeholder="Description" class="form-control"
                            value="<%- opening.description %>" />
                    </div>

                    <div class="form-group">
                        <label for="isOpen">Status</label>
                        <select name="isOpen" id="isOpen" class="form-control">
                            <% if(opening.isOpen) { %>
                            <option selected>Open</option>
                            <option>Closed</option>
                            <% } %>

                            <% if(!opening.isOpen) { %>
                            <option>Open</option>
                            <option selected>Closed</option>
                            <% } %>
                        </select>
                    </div>
                    <div>
                        <button type="submit" class="btn btn-primary btn-dark">Update</button>
                        <a class="btn btn-link text-black-50" href="#">Back</a>
                    </div>
                </form>
            </div>
        </div>
    </div>

</body>

</html>